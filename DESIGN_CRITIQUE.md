# Mise — дизайн-критика (2026-08-27)

Объект: PWA Mise, экраны собраны в `app/page.tsx`, оформление — `app/globals.css`.
Стадия: доведение MVP до закрытого пилота (этап 2 по `ROADMAP.md`).
Ревью по коду и CSS, без прогона на устройстве. Дополняет `UX_REVIEW.md` (там продукт и флоу),
здесь — первое впечатление, иерархия, консистентность, доступность.

## Общее впечатление

Светлый «стеклянный» iOS-стиль выдержан и выглядит дороже, чем типичный MVP: единая кремово-мятная
палитра, аккуратные скругления, честные формулировки про ориентировочность КБЖУ. Главная упущенная
возможность — интерфейс оформлен как презентация плана, а не как инструмент дня: самые крупные
элементы отвечают на вопрос «какой у меня план», а не «что мне сейчас делать». Вторая системная
проблема — визуальная иерархия держится на цвете и размере, но не на структуре: шкалы нет,
значения выбраны на глаз, и оттого второстепенное часто выглядит важнее главного.

## Юзабилити

| Находка | Важность | Рекомендация |
|---|---|---|
| Шаг «Выбор меню» требует 9–15 отдельных выборов из пяти карточек | 🔴 | Автосборка по `fitScore`, шаг превращается в «Меню собрано — проверьте», выбор из пяти остаётся как «Заменить» |
| Любая правка калорий/дат/приёмов пищи стирает всё выбранное меню без предупреждения | 🔴 | Пересчитывать порции на тех же блюдах, помечать разъехавшиеся позиции |
| Шаги мастера не в `history`: аппаратный «назад» закрывает PWA | 🔴 | `pushState` на шаг, `popstate` → `setStep` |
| Экран недели открывается на первом дне плана, а не на сегодняшнем | 🔴 | `clamp(today, start, end)` + автоскролл дата-стрипа |
| Нет состояния «план закончился» — критично для возврата за следующим планом | 🔴 | Экран завершения с «Повторить план» / «Составить новый» |
| «Составить» стоит вкладкой в таб-баре: задача в позиции раздела | 🟡 | 4 вкладки + FAB |
| Ежедневный вопрос «что достать, когда готовить» нигде не отвечен явно | 🟡 | Карточка «Сегодня» первым блоком недели |
| Дата-стрип до 14 дней без маркера «сегодня» и без границ партий | 🟡 | Точка «сегодня», группировка по партиям |
| Внутри партии три дня подряд показывают одно и то же блюдо без объяснения | 🟡 | Подпись «День 2 из 3 · то же блюдо», счётчик оставшихся контейнеров |
| Отметка в покупках сохраняется оптимистично, ошибка не показывается, undo нет | 🟡 | Откат чекбокса + тост, «снять все», отметка «есть дома» |
| Слайдеры порции меняют список покупок молча | 🟡 | «Покупки обновлены: рис +120 г» после сохранения |
| Селекты выбора человека и фильтров — 39–40px и шрифт 11px | 🟢 | 44px, шрифт 16px (иначе Safari зумит страницу при фокусе) |

## Визуальная иерархия

**Что видит глаз первым.** На экране недели — оранжевая выбранная дата в стрипе и крупный блок
макросов (числа 25–36px). Это не главное: в милпрепе главное — блюдо и действие с ним. Названия
блюд идут 17–18px, то есть атрибут крупнее объекта.

**Как движется взгляд.** kicker → h1 в шапке → дата-стрип → макро-герой → плашка готовки → меню дня.
Порядок логичный, но первые два экрана прокрутки заняты цифрами, которые нужны раз в день, а
карточки блюд — то, ради чего открывают приложение, — начинаются ниже сгиба.

**Акцент.** Оранжевый одновременно означает: главное действие (`primary-button`), выбранное
состояние (дата, день готовки, чипы) и просто надзаголовок (`.kicker` цветом `--accent-dark`).
Один цвет с тремя смыслами — глаз перестаёт считывать, где действие. Оставить оранжевый только за
единственным главным действием экрана; выбор перевести на мятный/нейтральный, киккеры — на
`--ink-soft`.

**Фон.** Три `ambient`-пятна плюс `backdrop-filter: blur(26px)` примерно на сорока поверхностях:
при прокрутке под карточками едет градиент, контраст текста меняется от позиции. Фон конкурирует
с контентом вместо того, чтобы его держать.

**Воздух.** Плотность высокая: карточки прилегают друг к другу с шагом 7–13px при внутренних
отступах 10–20px — внешний ритм слабее внутреннего, из-за чего блоки визуально слипаются.
Разнести секции до 24–32px.

## Консистентность

| Элемент | Что в коде | Рекомендация |
|---|---|---|
| Типографика | 12 разных размеров заголовков, `h2` от 19 до 36px, шкалы нет | Шкала 13 / 15 / 17 / 20 / 24 / 30 и роль на каждый шаг |
| Насыщенность | 12 значений `font-weight`: 300, 320, 500, 700, 720, 750, 760, 780, 800, 820, 850, 900 — при этом переменный шрифт не подключён, система округлит 750/760/780 к 700–800, и задуманные градации просто не отрисуются | Три веса: 400 / 600 / 800 |
| Скругления | Более 20 значений от 5px до 58px | Четыре токена: 12 / 16 / 24 / pill |
| Отступы | Подряд заняты почти все значения от 2px до 21px, сетки нет | Сетка 4px: 4 / 8 / 12 / 16 / 24 / 32 |
| Поверхности | ~40 полупрозрачных фонов с альфой от 0.35 до 0.94, каждый задан по месту | Три уровня поверхности в токенах + один `--glass` для навигации и модалок |
| Иконки | Типографские глифы (▦ ⌑ ♨ ◷ ✦ ◎ ▤) вперемешку с эмодзи (☀️ 🥗 🌙 💪 🌿) | Один inline-SVG набор 24×24, эмодзи только как «еда» в карточках блюд |
| Шрифт | `font-family: Inter` объявлен, но нигде не грузится: Apple отдаст SF Pro Rounded, Windows — Segoe UI, Android — Roboto | Подключить один шрифт (Geist уже лежит в `.vinext/fonts`) либо честно оставить системный стек |
| Выбранное состояние | В одних местах ✓, в других только заливка, в третьих тень | Единый паттерн: заливка + ✓ + рамка |

## Доступность

**Контраст** (WCAG 2.1 AA: 4.5:1 для текста меньше 18.5px, 3:1 для элементов интерфейса):

| Элемент | Цвет | На карточке | На фоне страницы |
|---|---|---|---|
| Белый текст на главной кнопке | градиент `#ff8b50 → #ed5b2b` | **2.62 – 3.42** ❌ | — |
| Белые бейджи К/Б/Ж/У, 9px | на `--accent #ff7840` | **2.62** ❌ | — |
| `.kicker`, 11px, uppercase | `--accent-dark #db5426` | **3.96** ❌ | **3.29** ❌ |
| Вторичный текст | `--muted #71817d` | **4.09** ❌ | **3.40** ❌ |
| Основной текст | `--ink #17302b` | 14.06 ✅ | 11.70 ✅ |
| Текст средней важности | `--ink-soft #344c47` | 9.25 ✅ | 7.70 ✅ |

Главная кнопка продукта не проходит даже порог для элементов интерфейса. Затемнить градиент
примерно до `#c2410c → #9a3412`, `--muted` до `#5c6b67`, киккеры перевести на `--ink-soft`.

**Тач-таргеты.** Основные кнопки 44–48px и bottom-nav 76px — в порядке. Ниже нормы: селекты
фильтров (40px) и выбора человека в карточке рецепта (39px).

**Читаемость.** Распределение кеглей: 7px — 1, 8px — 5, 9px — 18, 10px — 23, 11px — 23, 12px — 25,
против трёх десятков объявлений 13px и крупнее. Для приложения, которое читают на кухне с телефона
на столе, пирамида перевёрнута. Нижняя граница: 13px для вторичного текста, 15–16px для основного,
11px — только для капслочных киккеров.

**Прочее.** `alt` у фотографий рецептов есть, декоративная графика скрыта через `aria-hidden`,
иконочные кнопки подписаны `aria-label`, `:focus-visible` объявлен глобально — всё это хорошо.
Проблемы: `role="radiogroup"` с детьми-кнопками `aria-pressed` (скринридер объявит переключатели,
а не радиокнопки) и `.save-error`, который появляется под фиксированным футером мастера.
Тёмной темы нет ни одного объявления `prefers-color-scheme`.

## Что работает хорошо

- `DailyBalance` — честная и полезная механика: показывает разницу с целью, а не только «съедено»,
  объявлена как `role="status"`, формулировки без ложной точности.
- Раскладка по контейнерам с подписью «имя / приём пищи / даты» — это и есть обещанная ценность,
  и она оформлена лучше всего в приложении.
- `remainder-sheet`: остаток периода не округляется молча, а выносится в явное решение с тремя
  внятными вариантами и последствиями каждого.
- Отдельная обработка `loadError` вместо пустого экрана — редкая для MVP аккуратность.
- Поддержка `prefers-reduced-motion`, `prefers-reduced-transparency` и `env(safe-area-inset-*)`.
- Палитра и общий тон: продукт про режим питания не выглядит ни медицинским, ни спортзальным.

## Три приоритета

1. **Снять с пользователя сборку меню и перестать стирать его работу.** Шаг 5 из выбора
   превращается в проверку, правки в шагах 0–4 пересчитывают порции вместо обнуления селекций.
   Это одновременно самая большая экономия времени и устранение самого болезненного класса потерь.
2. **Переверстать «Неделю» вокруг вопроса «что сейчас».** Карточка «Сегодня» первым блоком:
   ближайший приём пищи, какой контейнер достать, что переложить из морозилки на завтра, сколько
   дней до следующей готовки. Макро-блок — ниже и компактнее, названия блюд крупнее чисел.
3. **Ввести токены и поднять читаемость.** Шкала кеглей, три веса, четыре радиуса, сетка 4px,
   три уровня поверхности; затемнить акцент до прохождения контраста, поднять нижнюю границу
   кегля до 13px, заменить глифы на SVG-набор. Это дешевле, чем кажется: почти всё лечится
   правкой токенов в начале `globals.css` и заменой значений по файлу.
