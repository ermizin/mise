# Mise — целевой аудит подбора меню перед пилотом

> Дата: 2026-08-31
> Ветка: `claude/menu-variety-protein-floor`, коммит `1f1c312` поверх `363ff99`
> Предмет: логика подбора рецептов и покрытие каталога. Редизайн, Android, ML и общий рефакторинг вне области.
> Каталог: `data/recipe-runtime-catalog.json`, 406 карточек в рантайме, 250 проходят production-предикат.

## 1. Главный вывод

Схлопывание вариантов вызвано **не объёмом каталога и не калорийным окном**, а тем, что пропорциональная
доля дневного белка, приходящаяся на приём пищи, использовалась Recipe Engine как **жёсткий порог
допустимости блюда**.

Дневная норма делится по слотам пропорционально калориям, поэтому день с 40% энергии из белка (это
собственный потолок мастера, `maximumProteinCalorieShare: 0.4`) требовал 40 г белка от завтрака на
400 ккал и 77 г от завтрака на 775 ккал. Обычный завтрак такой плотности внутри коридора −10%/+5%
по калориям не даёт. Всё, что не дотягивало, отбрасывалось как `constraints_unsatisfied`, а тот же
порог блокировал и путь «повторить целую порцию» для высоких целей. Итог: **3 завтрака на 2500–3500 ккал**
и пустые слоты перекусов на 1400 ккал.

Второй, отдельный и **не исправленный** дефект — срок хранения: при партии от 4 дней завтрак
схлопывается по данным каталога, а не по логике (раздел 6).

## 2. Карта кода подбора

| Стадия | Где | Что делает |
| --- | --- | --- |
| Цели по слотам | `domain/nutrition.ts:496` `allocateCalories`, `:520` `calculateMealPlanTargets` | Фиксированные доли 25/10/30/10/25. **Не зависят от числа выбранных приёмов пищи** — цель завтрака определяется только дневным калоражем |
| Цель конкретного слота | `app/page.tsx:4474` `targetFor` | Доля дня, пропорциональные БЖУ |
| Фильтр и ранжирование кандидатов | `app/page.tsx:5581` `candidateRecipes` | Приём пищи → тег направления → скрытые диеты → production-предикат → хранение/заморозка → Recipe Family = `pilot` → жёсткие исключения → «не люблю» → фильтры каталога → **проверка solver'ом** |
| Ранжирование | `app/page.tsx:5776` `fitScoreForSession`, `:5516` `styleScore`, `:5629` `combinationBonus` | `fit*4 + styleScore + 12 за parsed`; при сборке добавляется бонус за общие продукты и штраф −240 за повтор |
| Автосборка меню | `app/page.tsx:5659` `automaticAssignmentsFor`, `:5637` `chooseMenuCandidate` | Сначала общее блюдо на всех, иначе персональные назначения |
| Порция | `app/page.tsx:4793` `portionFor` → `:4718` `solveRecipeFamilyMeal` | Строит вход солвера; при неудаче — путь «повторить целую порцию» |
| Допустимость | `app/page.tsx:4940` `recipeCookingSession` | `viable` = все порции решены движком; иначе рецепт исчезает из выдачи |
| Солвер | `domain/recipe-engine.ts:2955` `solveRecipeFamilyUncached`, `:2809` `hillClimb`, `:2780` `scoreFor` | Окно `[0.9T, 1.05T]` по калориям + порог по белку |
| Границы ролей | `domain/recipe-engine.ts:2121` `bounds` | Универсальные множители на роль ингредиента |
| Рабочий диапазон блюда | `domain/recipe-engine.ts:2349` | `minViableCalories`/`maxViableCalories` из достижимости ингредиентов |

## 3. Трассировка: 1600 и 3100 ккал, 40% энергии из белка, партия 3 дня, завтрак

| Условие | 1600 ккал (цель 400 ккал / 40 г) | 3100 ккал (цель 775 ккал / 77 г) |
| --- | --- | --- |
| весь каталог | 406 | 406 |
| подходит по приёму пищи | 74 | 74 |
| тег направления | 53 | 53 |
| не скрытая диета | 51 | 51 |
| production-ready | 36 | 36 |
| хранение ≥ 3 дней или заморозка | 30 | 30 |
| Recipe Family = `pilot` | 30 | 30 |
| **solver viable — было** | **9** | **3** |
| solver viable — стало | 11 | 13 |

Причины отбраковки на последнем шаге (было → стало):

| Причина | 1600 | 3100 |
| --- | --- | --- |
| `constraints_unsatisfied` (не добирает белок) | 16 → 14 | 14 → 6 |
| `outside_calorie_range`, цель ниже минимума блюда | 4 → 4 | 0 → 0 |
| `outside_calorie_range`, цель выше потолка блюда | 1 → 1 | 13 → 11 |

Порог по белку — единственная причина, которая двигается. Расширение калорийного окна до −15%/+15%
в измерениях **не добавляет ни одного кандидата** ни в одной ячейке сетки: окно не является узким местом.

## 4. Что именно схлопывало variety

| Гипотеза | Вердикт |
| --- | --- |
| Слишком узкое окно по калориям | **Нет.** Расширение до ±15% даёт 0 новых кандидатов |
| Жёсткий порог по белку на приём пищи | **Да, основная причина.** Снятие порога поднимает завтраки с 3 до 17–30 |
| Фиксированные доли калорий по приёмам | Косвенно: доли не перенормируются, поэтому плотность белка в слоте равна дневной |
| Границы масштабирования порций | Частично, на краях: `outside_calorie_range` — 13 из 30 завтраков на 3100 ккал |
| Порядок фильтров / раннее pruning | Нет. Порядок влияет только на стоимость, не на результат |
| Meal-type tagging | Нет для завтрака (74 карточки), **да для перекусов** (12 карточек, 4 production-ready) |
| Малый каталог | Нет на 1600–3100 ккал; да на 1200 ккал (13 из 30 завтраков не сжимаются до 300 ккал) |
| Конфликт ограничений | Нет; жёсткие исключения в измерениях не задавались |

## 5. Фикс

Идея: **доля белка — свойство дня, а не каждого блюда**. Она остаётся целью оптимизации, но перестаёт
быть воротами допуска, когда превышает то, что блюдо такого размера физически может нести.

| Файл | Изменение |
| --- | --- |
| `domain/recipe-engine.ts` | `SolveRecipeFamilyInput.proteinFloor?: number` — явный порог допустимости, по умолчанию равен `targetProtein`, так что поведение прежних вызовов не меняется. Учтён в ключе кэша. `scoreFor` по-прежнему штрафует недобор относительно полной цели, поэтому солвер выдаёт максимум белка, который блюдо может нести |
| `domain/nutrition.ts` | `macroPolicy.mealProteinFloorCalorieShare: 0.32` и `mealProteinFloor(kcal, protein)` |
| `app/page.tsx` | `portionFor` передаёт полную пропорциональную долю как `targetProtein` и `mealProteinFloor(...)` как `proteinFloor`; путь повторной порции делит и порог на число повторов |
| `lib/nutrition-engine-v2.ts` | реэкспорт |

Порог 0,32 выбран по замеру: это наименьшее ослабление, которое возвращает завтраку двузначное число
вариантов на 2500–3100 ккал и при этом удерживает дневной белок в пределах −2% от цели. При 0,30
(сбалансированный профиль) поведение вообще не меняется — порог равен доле.

## 6. Before / after

Партия 3 дня, направление «высокобелковое», сетка 1200–3500 ккал × 2/3/4/5 приёмов пищи.

Дневная норма 40% энергии из белка (потолок мастера):

| Ккал/сут | Завтрак | Обед | Ужин | Пустые слоты |
| --- | --- | --- | --- | --- |
| 1200 | 3 → 4 | 22 → 24 | 12 → 13 | 0 → 0 |
| 1400 | 5 → 8 | 28 → 31 | 21 → 21 | 3 → 3 (перекусы) |
| 1600 | 9 → 11 | 26 → 33 | 32 → 35 | 0 → 0 |
| 1800 | 9 → 12 | 29 → 37 | 32 → 38 | 0 → 0 |
| 2000 | 10 → 12 | 25 → 38 | 29 → 40 | 0 → 0 |
| 2200 | 8 → 13 | 21 → 39 | 31 → 42 | 0 → 0 |
| 2500 | **3 → 13** | 23 → 34 | 24 → 44 | 0 → 0 |
| 2800 | **3 → 13** | 26 → 33 | 24 → 41 | 0 → 0 |
| 3100 | **3 → 13** | 23 → 30 | 29 → 39 | 0 → 0 |
| 3500 | 3 → 5 | 23 → 30 | 31 → 37 | 0 → 0 |

Дневная норма 30% энергии из белка: **все 40 измеренных ячеек идентичны** до и после.

Точность:

| Метрика | Было | Стало |
| --- | --- | --- |
| Дневной белок против цели, 40% профиль | 0…+7% | −2…0% |
| Дневные калории против цели | −0,1…+1,5% | −0,1…+1,5% |
| Отклонение порции по калориям (2220 порций) | внутри −10%/+5% | −9,1%…+5,0%, внутри контракта |
| Уникальных завтраков на 3 партии | 3 из 3 | 3 из 3 |
| Повторы рецептов внутри приёма пищи | 0 | 0 |

## 7. Тесты

- `tests/automatic-menu-coverage.test.mjs` — новый тест «high-protein daily targets keep real menu variety
  at 1600 and 3100 kcal»: на обоих направлениях и партиях 1 и 3 дня требует минимум 6 завтраков и
  15 обедов/ужинов и проверяет, что каждая предложенная порция остаётся внутри −10%/+5%.
  На прежней политике тест падает (`3100kcal protein d3 breakfast: 3 < 6`).
- `tests/nutrition.test.mjs` — модульный тест `mealProteinFloor`.
- Прогон: `node --test tests/*.test.mjs` — 215 тестов, 0 падений. `tsc --noEmit` и `eslint` чисто.

## 8. Что осталось риском

1. **Срок хранения обнуляет завтрак на длинных партиях.** Из 41 production-ready завтрака только
   2 помечены `freezable`. При партии 4 дня остаётся 11 карточек, при 5–7 дней — **1**, независимо от
   калоража. Мастер предлагает партии до 7 дней, а обещание продукта — «Готовим раз — едим всю неделю».
   Это дефект данных каталога, а не логики; правка требует редакционного решения владельца
   (какие завтраки допустимо замораживать) и потому осталась вне этого изменения.
2. **Перекусы.** 12 карточек `snack1` в каталоге, 4 проходят production-предикат, на 1200–1600 ккал
   допустимых 0–1. При 4–5 приёмах пищи и высоком белке автосборка оставляет слот пустым.
3. **Нижний край 1200 ккал.** 13 из 30 завтраков не сжимаются до 300 ккал (`minViableCalories`).
   Честный ответ на это — сателлит-модуль из `RECIPE_HANDOFF.md` §3.6, он меняет контракт порции.
4. **Верхний край 3500 ккал при 40% белка.** 5 завтраков. Дальше упирается уже в каталог, а не в логику.
5. **Контракт.** `PRODUCT.md` говорит «сохраняя минимум белка» — минимум семейства (`family.minimumProtein`)
   остался жёстким. Пропорциональная доля слота нигде в контракте жёстким порогом не объявлена, поэтому
   формального конфликта нет, но при freeze это стоит подтвердить владельцем и, при желании, зафиксировать
   строкой в `PRODUCT.md`.
6. В общем чекауте есть чужой заблокированный worktree `claude/recipe-engine-fixes`; перед интеграцией
   стоит убедиться, что он не трогает те же функции.

## 9. Что не трогать до релиза

- Калорийное окно `[0.9T, 1.05T]` в `solveRecipeFamily` — измерения показывают, что оно ничего не блокирует,
  а его расширение сразу ломает контракт по точности порции.
- `bounds()` и порецептурные переопределения границ ролей (`RECIPE_HANDOFF.md` §3.4, P2) — это отдельная
  задача с собственным regression-риском.
- Доли калорий по приёмам пищи 25/10/30/10/25 и отказ от их перенормировки — продуктовое решение.
- Состав каталога, источники, `freezable`, статусы `review_required`/`blocked` — только через редакционный
  проход с решением владельца.
- Геометрия и автоматические cooking splits — удалены решением владельца 2026-08-30.
